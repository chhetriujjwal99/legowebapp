<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Set</title>
    <link href="/css/main.css" rel="stylesheet">
    <style>
        /* General Styles for Forms */
form {
  max-width: 700px;
  margin: 2rem auto;
  padding: 2rem;
  background: linear-gradient(145deg, #ffffff, #f3f3f3);
  border-radius: 12px;
  box-shadow: 0 8px 12px rgba(0, 0, 0, 0.15);
  font-family: 'Poppins', sans-serif;
  transition: all 0.3s ease-in-out;
}

form:hover {
  transform: scale(1.02);
}

/* Form Header */
form h1 {
  font-size: 2rem;
  margin-bottom: 1.5rem;
  text-align: center;
  color: #222;
  letter-spacing: 1px;
  text-transform: uppercase;
}

/* Form Labels */
form label {
  display: block;
  margin-bottom: 0.5rem;
  font-weight: 600;
  color: #444;
  font-size: 1rem;
}

/* Form Inputs */
form input,
form select,
form textarea {
  width: 100%;
  padding: 0.9rem;
  margin-bottom: 1.5rem;
  border: 1px solid #ddd;
  border-radius: 6px;
  font-size: 1rem;
  background-color: #fff;
  color: #333;
  box-sizing: border-box;
  transition: border-color 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
}

form input:focus,
form select:focus,
form textarea:focus {
  border-color: #007bff;
  box-shadow: 0 0 6px rgba(0, 123, 255, 0.25);
  outline: none;
}

/* Placeholder styling */
form input::placeholder,
form textarea::placeholder {
  color: #bbb;
  font-style: italic;
}

/* Readonly Inputs */
form input[readonly] {
  background-color: #f1f3f5;
  cursor: not-allowed;
  border-color: #e0e0e0;
}

/* Form Buttons */
form button {
  display: inline-block;
  width: 48%;
  padding: 0.9rem;
  margin: 0.5rem 1%;
  border: none;
  border-radius: 6px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  text-align: center;
  color: #fff;
  transition: background-color 0.3s ease, transform 0.2s ease-in-out;
}

form button:hover {
  transform: translateY(-3px);
}

/* Primary Button */
form button[type="submit"] {
  background: linear-gradient(145deg, #007bff, #0056b3);
}

form button[type="submit"]:hover {
  background: linear-gradient(145deg, #0056b3, #003f7f);
}

/* Danger Button */
form button.btn-error {
  background: linear-gradient(145deg, #dc3545, #a71d2a);
}

form button.btn-error:hover {
  background: linear-gradient(145deg, #a71d2a, #720f1b);
}

/* Success Button */
form button.btn-success {
  background: linear-gradient(145deg, #28a745, #1c7430);
}

form button.btn-success:hover {
  background: linear-gradient(145deg, #1c7430, #155d22);
}

/* Error Message Styling */
.error-message {
  color: #dc3545;
  font-weight: bold;
  margin-bottom: 1rem;
  font-size: 0.9rem;
}

/* Responsive Design */
@media (max-width: 768px) {
  form {
    padding: 1.5rem;
  }

  form button {
    width: 100%;
    margin: 0.5rem 0;
  }
}

/* Additional Enhancements for Small Inputs */
form input[type="number"],
form input[type="url"],
form input[type="text"] {
  font-family: 'Roboto Mono', monospace;
}


    </style>
</head>
<body>
    <%- include('partials/navbar', {page: ''}) %>

    <h1>Edit Set: <%= set.name %></h1>

<form action="/lego/editSet" method="POST" class="grid grid-cols-2 gap-4 p-6 border rounded bg-gray-50">
    <input type="hidden" name="set_num" value="<%= set.set_num %>">
    
    <div>
        <label for="name" class="block text-sm font-medium">Name:</label>
        <input type="text" id="name" name="name" value="<%= set.name %>" class="w-full border p-2 rounded" required>
    </div>
    
    <div>
        <label for="year" class="block text-sm font-medium">Year:</label>
        <input type="number" id="year" name="year" value="<%= set.year %>" class="w-full border p-2 rounded" required>
    </div>
    
    <div>
        <label for="num_parts" class="block text-sm font-medium">Number of Parts:</label>
        <input type="number" id="num_parts" name="num_parts" value="<%= set.num_parts %>" class="w-full border p-2 rounded" required>
    </div>
    
    <div>
        <label for="img_url" class="block text-sm font-medium">Image URL:</label>
        <input type="url" id="img_url" name="img_url" value="<%= set.img_url %>" class="w-full border p-2 rounded" required>
    </div>
    
    <div>
        <label for="theme_id" class="block text-sm font-medium">Theme:</label>
        <select id="theme_id" name="theme_id" class="w-full border p-2 rounded" required>
            <% themes.forEach(theme => { %>
                <option value="<%= theme.id %>" <%= (set.theme_id == theme.id) ? "selected" : "" %>>
                    <%= theme.name %>
                </option>
            <% }) %>
        </select>
    </div>
    
    <div>
        <label for="set_num" class="block text-sm font-medium">Set Number:</label>
        <input type="text" id="set_num" name="set_num" value="<%= set.set_num %>" class="w-full border p-2 rounded cursor-not-allowed" readonly>
    </div>
    
    <div class="col-span-2">
        <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600">
            Update Set
        </button>
    </div>
</form>





</body>
</html>
